#include <cmath>
#include "perfshuffle.hpp"

void perfshuffle(double* inArray, int numElems)
{
	bool isEven = (numElems%2 == 0);
	int idxStart = 0;
	int idxMid = floor(numElems/2) + 1;

	for(int i = 0; i < 
}
